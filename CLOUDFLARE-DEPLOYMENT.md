# VerifyAI - Cloudflare Pages 部署指南

本指南將協助你將 VerifyAI 網站部署到 Cloudflare Pages 免費服務。

## 📋 前置需求

1. GitHub 帳號
2. Cloudflare 帳號（免費）- 如果沒有，請到 https://dash.cloudflare.com/sign-up 註冊
3. Git 已安裝在本地電腦

## 🚀 部署步驟

### 方法一：通過 Cloudflare Dashboard（推薦）

#### 步驟 1: 推送程式碼到 GitHub

確保你的程式碼已經推送到 GitHub repository。

```bash
# 如果還沒有推送，執行以下命令
git add .
git commit -m "Prepare for Cloudflare Pages deployment"
git push -u origin claude/deploy-to-cloudflare-011CV3aykKKKhxPKgdM9iaqS
```

#### 步驟 2: 登入 Cloudflare Dashboard

1. 前往 https://dash.cloudflare.com/
2. 登入你的 Cloudflare 帳號
3. 點擊左側選單的 **"Workers & Pages"**

#### 步驟 3: 建立新的 Pages 專案

1. 點擊 **"Create application"** 按鈕
2. 選擇 **"Pages"** 標籤
3. 點擊 **"Connect to Git"**

#### 步驟 4: 連接 GitHub Repository

1. 選擇 **"GitHub"** 作為 Git 提供者
2. 授權 Cloudflare 訪問你的 GitHub 帳號
3. 選擇 **verifyai-main** repository
4. 點擊 **"Begin setup"**

#### 步驟 5: 配置構建設定

在構建設定頁面，填寫以下資訊：

- **專案名稱**: `verifyai`（或你喜歡的名稱）
- **生產分支**: `main`（或你的主要分支名稱）
- **構建命令**: 留空（因為這是純靜態網站）
- **構建輸出目錄**: `/`（根目錄）

#### 步驟 6: 環境變數（可選）

這個專案不需要環境變數，可以跳過此步驟。

#### 步驟 7: 完成部署

1. 點擊 **"Save and Deploy"** 按鈕
2. Cloudflare 將開始構建和部署你的網站
3. 等待 1-2 分鐘，部署完成後會顯示網站 URL

### 方法二：使用 Wrangler CLI（進階）

如果你熟悉命令列工具，可以使用 Wrangler CLI 進行部署。

#### 安裝 Wrangler

```bash
npm install -g wrangler
```

#### 登入 Cloudflare

```bash
wrangler login
```

#### 部署網站

```bash
wrangler pages deploy . --project-name=verifyai
```

## 🌐 自訂網域（可選）

部署完成後，Cloudflare 會提供一個免費的 `.pages.dev` 網域。如果你想使用自訂網域：

1. 在 Cloudflare Dashboard 中，前往你的 Pages 專案
2. 點擊 **"Custom domains"** 標籤
3. 點擊 **"Set up a custom domain"**
4. 輸入你的網域名稱
5. 按照指示配置 DNS 記錄

## ⚙️ 進階配置

### 自動部署

Cloudflare Pages 會自動監聽你的 GitHub repository：

- **生產環境**: 每次推送到主分支時自動部署
- **預覽環境**: 每個 Pull Request 都會建立預覽部署

### 環境變數

如果未來需要添加環境變數：

1. 前往 Pages 專案的 **"Settings"** 標籤
2. 選擇 **"Environment variables"**
3. 新增變數並選擇適用的環境（生產/預覽）

### 構建配置

如果未來需要更複雜的構建流程，可以在專案根目錄建立 `wrangler.toml` 檔案：

```toml
name = "verifyai"
pages_build_output_dir = "."

[site]
bucket = "."
```

## 📊 部署後檢查清單

- [ ] 確認網站可以正常訪問
- [ ] 測試所有頁面功能
- [ ] 檢查多語言切換是否正常
- [ ] 確認所有圖片和資源都能正確載入
- [ ] 測試在不同裝置上的顯示效果

## 🔧 常見問題

### Q: 部署後頁面顯示 404

**A:** 確認構建輸出目錄設定為 `/` 或 `.`，因為這是純靜態網站。

### Q: JavaScript 或 CSS 無法載入

**A:** 檢查檔案路徑是否正確。Cloudflare Pages 區分大小寫，確保檔案名稱完全匹配。

### Q: 多語言功能不工作

**A:** 確認 `locales/` 目錄和所有 JSON 檔案都已正確上傳。檢查瀏覽器控制台是否有錯誤訊息。

### Q: 如何查看部署日誌

**A:** 在 Cloudflare Dashboard 的 Pages 專案頁面，點擊 **"Deployments"** 可以查看所有部署記錄和日誌。

### Q: 如何回滾到先前的版本

**A:** 在部署記錄中找到想要回滾的版本，點擊 **"Rollback to this deployment"**。

## 📈 效能優化建議

Cloudflare Pages 已經提供以下優化：

- ✅ 全球 CDN 分發
- ✅ 自動 HTTPS
- ✅ HTTP/2 和 HTTP/3 支援
- ✅ 自動壓縮（Gzip/Brotli）
- ✅ 智慧路由

你可以進一步優化：

1. **圖片優化**: 使用 Cloudflare Images 或將圖片轉換為 WebP 格式
2. **快取策略**: 在 `_headers` 檔案中配置快取規則
3. **預載資源**: 使用 `<link rel="preload">` 預載重要資源

## 🔒 安全性

Cloudflare Pages 提供的安全功能：

- 免費 SSL/TLS 證書
- DDoS 防護
- Web Application Firewall (WAF) - 付費方案
- Access Control - 可以設定密碼保護

## 💰 費用說明

Cloudflare Pages 免費方案包含：

- ✅ 無限制的請求
- ✅ 無限制的頻寬
- ✅ 500 次構建/月
- ✅ 1 次並發構建
- ✅ 無限制的網站數量

這對大多數個人和小型專案來說完全足夠！

## 📞 需要協助？

- Cloudflare Pages 文件: https://developers.cloudflare.com/pages/
- Cloudflare 社群: https://community.cloudflare.com/
- GitHub Issues: https://github.com/yourusername/verifyai-main/issues

## 🎉 完成！

恭喜！你的 VerifyAI 網站現在已經部署到 Cloudflare Pages。

你的網站 URL 會是類似：`https://verifyai.pages.dev`

享受快速、安全、免費的全球網站託管服務！
